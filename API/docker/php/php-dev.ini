; Dev overrides (mounted into conf.d). Mirror the prod upload limits so avatar/
; photo uploads (capped at 10M in the domain) aren't rejected by PHP defaults.
memory_limit = 256M
post_max_size = 16M
upload_max_filesize = 16M
